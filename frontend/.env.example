# Production (Render API) — either form works; client auto-appends /api/v1 if missing
VITE_API_URL=https://royal-rail-restro-api.onrender.com/api/v1

# Local Docker / Vite proxy
# VITE_API_URL=/api/v1
